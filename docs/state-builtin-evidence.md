# Shell state and builtin evidence

[CSH-048](tickets/CSH-048-state-builtin-evidence.md) reviews the 24 families below
against POSIX.1-2024. This inventory maps individual conditions to executable
assertions; it does not promote an entire family to verified from selected
witnesses. The [run records](#run-records) identify tested source and binaries.

## Sources and execution contract

Reviewed on 2026-09-26: [Shell Command Language §2.5.3](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#tag_19_05_03)
(variables), [§2.8.1](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#tag_19_08_01)
(errors), [§2.9.1](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#tag_19_09_01)
(assignments/lookup), [§2.13](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#tag_19_13)
(environments), [§2.15](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#tag_19_15)
(individual special utilities), [sh ENVIRONMENT VARIABLES](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/sh.html),
and each linked utility's DESCRIPTION, OPTIONS, OPERANDS, ENVIRONMENT VARIABLES,
STDOUT, STDERR, EXIT STATUS and CONSEQUENCES OF ERRORS. The source is Issue 8,
including `cd -e`, `read -d`, getopts/read error status distinctions and the
expanded base `ulimit` resource inventory. The fetched source hashes are in
[the evidence directory](evidence/csh-048/README.md).

Fixture labels in the tables resolve as follows. Names are exact, except for the
stated prefixes and three mode suffixes. Each generator retains literal script,
setup, expected stdout/stderr/status and filesystem assertions.

| Label | Fixture and execution |
| --- | --- |
| S | [`state_builtin_cases.py`](../tests/state_builtin_cases.py), prefix `state-builtin: `, suffix `(string)`, `(file)` or `(stdin)`. Generated into both `state-builtins.json` and `runtime.json`. |
| P | [`state_builtin_observations.py`](../tests/state_builtin_observations.py), prefix `state-observation: ` and the same mode suffixes; explicit predicates for times, limits, hash output and sorted listing. |
| E | [`evaluation_cases.py`](../tests/evaluation_cases.py), prefix `evaluation: ` and the same mode suffixes. |
| R | [`runtime_cases.py`](../tests/runtime_cases.py); cross-mode names have the same suffixes, PTY and explicitly interactive cases retain their printed names. |
| C | [`control_flow_cases.py`](../tests/control_flow_cases.py), prefix `control: ` and the same mode suffixes. |
| O / T | [`option_cases.py`](../tests/option_cases.py), prefix `options: `; [`trap_cases.py`](../tests/trap_cases.py), prefix `traps: `; same cross-mode suffixes. |
| API | [`state_fixture.c`](../tests/state_fixture.c), [`builtin_fixture.c`](../tests/builtin_fixture.c), [`assignment_fixture.c`](../tests/assignment_fixture.c), and [`builtins.py`](../tests/builtins.py). These use storage or prepared/literal executor interfaces, not the public runtime. Function names or literal scripts identify assertions below. |

S/P/E/R/C/O/T use the bounded shared runner with isolated HOME/TMPDIR, PATH from
`os.defpath`, LANG/LC_ALL=C (unless overridden explicitly), umask 077, fresh cwd,
five-second deadline, 65,536-byte captured output bound, and process-group
cleanup. P also sets TZ=UTC0. P checks status 0, empty stderr and no runner
failures **in addition** to its stdout predicate. Limit mutations occur only
inside disposable shell children; the test runner's limits are unchanged.
`state_builtin_helper.c` burns 0.15 CPU seconds or reports actual `getrlimit`
values, independently of cshell formatting. API evidence never substitutes for
runtime coverage. Host printf/cat/test/mkdir/ln supply fixture operations, not
oracles for shell semantics.

Exact cshell diagnostic wording and chosen nonzero statuses are regression
assertions. Unless explicitly specified, the normative contract is a diagnostic
and the required status class/consequence, not that wording. No reference shell
was used to derive expectations. E's historical exact hash lines and numeric
umask spelling are project regression choices; P/S add format-independent
invariants and reinput tests instead of treating those spellings as portable.

For U-001's utility-syntax partition, dot, exec, export, readonly and unset
claim the syntax guidelines (dot excepts the utility-name guidelines); set
adds its explicit + and -- rules. Colon, eval, return, shift and times do not
claim those guidelines: accepting an initial -- for some of them is a project
extension, not a blanket POSIX requirement. Error consequences still follow
§2.8.1 and each utility's specific exceptions.

## State and startup

Implementation: [`state.c`](../src/state.c) imports and owns values/attributes;
[`main.c`](../src/main.c) invokes `csh_builtin_initialize()` from
[`builtin.c`](../src/builtin.c) before parsing. Raw `csh_state_create()` remains a
storage API: it deliberately does not reset imported values. The runtime now
resets IFS/PPID/OPTIND and validates/initializes PWD. [`execute.c`](../src/execute.c)
owns execution contexts and temporary variable scopes.

| Row / clause and condition | Exact assertions | Remaining scope / classification |
| --- | --- | --- |
| <a id="env-001"></a>[ENV-001](posix-matrix.md#env-001): §2.5.3 import valid names with export; unset versus empty; attribute lifetimes | S `import export empty and unset` observes `a=b c`, empty-but-set, absent, inherited helper environment and removal after unset. S `readonly attribute not inherited` changes an exported readonly parent's value in a new shell while parent remains unchanged. API `variables_and_import`, `attributes_and_snapshots` assert owned copies, exported unset omission, empty inclusion, readonly rejection and independent environment snapshots. | Non-name environment entries are ignored by the storage API; duplicate entries use last-wins policy outside portable environment guarantees. Public-runtime malformed envp and allocator-failure startup injection remain CSH-048. |
| <a id="env-003"></a>[ENV-003](posix-matrix.md#env-003): §2.5.3 IFS reset; PPID initialization/subshell stability; PWD validation | S `startup IFS reset ''` / `':hostile:'` assert exact space/tab/newline and two fields. `startup PPID and subshell stability` compares against the actual invoking shell PID even with a forged imported PPID. `startup missing PWD`, `startup invalid PWD ''` / `'relative'` / `'/not-the-current-directory'`, `startup logical PWD retained`, `startup dot PWD normalized` assert physical fallback or mandatory valid symlink-path retention. `startup OPTIND reset` asserts 1 despite imported 99. | Valid logical PWD is retained even beyond PATH_MAX (permitted alternative); an unavailable physical cwd selects unset (unspecified case). Permission-denied/deleted cwd and over-PATH_MAX runtime probes remain CSH-048. PWD is exported; imported attributes on reset variables persist. |
| ENV-003: HOME, PATH and internationalization precedence | S `cd HOME and OLDPWD`; E `command default PATH preserves environment`, `command intrinsic without PATH`; [`portability.py`](../tests/portability.py) `LC_ALL overrides LC_CTYPE`, `LC_CTYPE when LC_ALL is empty`; [`locale_cases.py`](../tests/locale_cases.py) `locale runtime LANG and LC_ALL precedence`, `locale temporary builtin assignment restores categories`, `locale subshell and substitution changes stay local`. These assert exact directory, lookup, multibyte matching and restoration outcomes. | Locale names are capability-probed. Native translated-libc diagnostic skip is owned by CSH-042; Linux exercises it. cshell's own English diagnostics have no catalogs. Non-UTF-8 lexical boundaries remain CSH-053; expansion breadth remains CSH-047. |
| <a id="env-005"></a>[ENV-005](posix-matrix.md#env-005): source-conditional variables versus base consumers | The §2.5.3 **processing** paragraphs for ENV, LINENO, PS1, PS2 and PS4 are UP; sh's FCEDIT/HISTFILE/HISTSIZE, editing and MAIL/MAILCHECK/MAILPATH branches are UP; NLSPATH's shell/catalog branch is XSI. These portions alone are inapplicable under the selected profile. Base `set -x` still requires its trace behavior: O `O-016 expanded trace`, `O-016 stderr destination`, `O-016 substitution trace` assert expanded arguments, prefix and stderr. Base `read` PS2 and here-document prompting remain applicable: R `terminal read continuation prompt` and `terminal heredoc prompts` assert exact continuation output on controlling PTYs. | Imported variables still obey ENV-001. Do not exclude the entire family or base prompt/trace consumers. cshell's optional primary prompts and line numbers are extensions under this profile, not evidence of selecting all UP. Extended prompt expansion/history, interactive ENV loading and catalogs must be reconsidered if UP/XSI is selected. Detailed base option obligations remain CSH-051; base read prompt expansion beyond literal PS2 remains CSH-048. |
| <a id="env-006"></a>[ENV-006](posix-matrix.md#env-006): §2.13 current versus subshell environments | S `context isolation subshell` / `substitution` / `pipeline` / `background` mutate cwd, variable, umask, function, alias and noglob together; assert unchanged parent state, no leaked definitions and successful wait. `current environment brace dot eval function` asserts four persistent assignments. P `ulimit independent soft hard and subshell` asserts child-only limit changes. E `exec no command persistent fd`, `exec redirects in eval unwind outer only` assert descriptor lifetime and exact files. T `caught action resets in subshell`, `external signal dispositions`, `function changes shell traps` assert trap/disposition inheritance. | D-007 deliberately isolates every multi-stage pipeline stage. Known child IDs and trap reset subtleties retain [CSH-050's clause map](jobs-signals-evidence.md) and CSH-054 residual owners. Complete cross-product of nested contexts, descriptors and failures is not asserted; additional CSH-048 coverage remains open. |

## Special utilities

Implementation: [`builtin.c`](../src/builtin.c) handles attributes, set, shift,
unset and directory utilities; [`prepare.c`](../src/prepare.c) selects declaration
contexts; [`execute.c`](../src/execute.c) handles dispatch, dot/eval/exec/return;
[`utility.c`](../src/utility.c) handles times. Normative sources below point to
individual entries in §2.15, including their utility-specific syntax rules.

| Row / reviewed clause and condition | Exact witnesses and assertions | Policy / remaining limitation |
| --- | --- | --- |
| <a id="u-001"></a>[U-001](posix-utilities.md#u-001): §2.15 and §§2.8.1/2.9.1 special lookup, assignments, errors and redirected output | S `special assignment persistence and output`, `special redirect failure stops`, `readonly assignment through export stops`; R `special builtin error stops script`, `interactive special builtin error (string)` / `(file)`; E `command suppresses special assignment`, `command special error continues`, `command redirection error continues input`. Assert persistence, absent post-error file, exact continuation and restored output. C `function special name rejected` asserts rejection before effects. API assignment `handler` and category/failure loops check selective restoration, attributes and nested handler failure. | D-006 prefix assignments persist for specials but do not acquire export solely from the prefix; ordinary/function prefixes restore. Syntax exceptions are per utility: `:` ignores `--`/`-z`; dot supports the required `--` delimiter; eval does not claim the syntax guidelines; set has its own +/-- rules. Exhaustive option/error/output-failure combinations for every special remain CSH-048. |
| <a id="u-002"></a>[U-002](posix-utilities.md#u-002): [colon](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#colon), expansion/redirections, ignored operands, success | S `colon expands ignores and redirects` asserts `${a:=value}` side effect, empty created file and status 0 with `-z --`; `special redirect failure stops` covers failure before the no-op handler. | `-z` is an ignored ordinary argument; `--` is a project regression input outside the conforming application rules for this special utility. No option parsing. Expansion-specific failure partitions remain CSH-047/049. |
| <a id="u-005"></a>[U-005](posix-utilities.md#u-005): [dot](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#dot), readable PATH source/current state, status and return | E `dot PATH and parameters`, `dot no args keeps changes to positionals`, `dot last failure continues`, `dot empty`, `dot does not search cwd implicitly`, `dot missing operand`, `nested dot return`, `dot input separate from read`, `dot supports readable device input`; S `dot parse error stops`. Assert current-state mutations, statuses 7/1/0/9, source-vs-stdin separation and absent post-syntax-error effect. | Extra dot positional operands and restoration are an extension in the first E case; its readable non-executable PATH search is baseline. No implicit cwd fallback is the selected policy. Mid-read I/O errors and denied-first/readable-later PATH candidates remain CSH-048. |
| <a id="u-006"></a>[U-006](posix-utilities.md#u-006): [eval](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#eval), space joining/current context, empty/nested status/errors | E `eval joins and shares state`, `eval empty succeeds`, `eval ordinary failure continues`, `eval parse failure`, `eval return leaves function`, `eval exit`; S `eval nested state and status`. Assert joined value, zero on no/empty operands, retained ordinary failure 1, fatal parse status 2, return 7 and exit 13. R `terminal eval syntax error recovers` verifies interactive recovery. | Nested allocation/read failures remain CSH-048; ordinary false is not a special-builtin error. |
| <a id="u-007"></a>[U-007](posix-utilities.md#u-007): [exec](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#exec), replacement, inheritance, no-command descriptors, lookup failures | E `exec same PID`, `exec inherits prefix environment`, `exec no command persistent fd`, `exec standard output persists`, `exec missing command`, `exec permission failure`, `exec pipeline isolation`, `exec end options permits dash name`. Assert PID identity, inherited value, permanent fds/files, 127/126 and no following execution. | Redirection ordering, executable-format fallback and unusual exec failures also belong to CSH-049. Interactive replacement failure matrix remains CSH-048. |
| <a id="u-009"></a>[U-009](posix-utilities.md#u-009): [export](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#export), attributes/assignments, -p reinput, declarations, errors | S `export listing round trip` restores apostrophe, empty and unset variables in a new shell; `export declaration fields` retains spaces/glob literally; `export invalid name stops`, `readonly assignment through export stops`; ENV-001 helper assertions establish actual child environment inclusion/removal. API builtin rows assert readonly/export attribute combinations unchanged on failure. | No-operand listing is the selected unspecified choice. Listing write failures, every syntax permutation and non-C listing order remain CSH-048. |
| <a id="u-010"></a>[U-010](posix-utilities.md#u-010): [readonly](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#readonly), protection, -p reinput, declarations, inherited values | S `readonly listing round trip`, `readonly declaration fields`, `readonly invalid name stops`, `readonly attribute not inherited`, `unset readonly stops`; API builtin rows verify failed reassignment/unset leaves both value and attributes intact. | No-operand listing is a selected unspecified choice. Runtime listing round trip checks values/setness; repeated readonly assignments, output failures and full reinput attribute breadth remain CSH-048. |
| <a id="u-011"></a>[U-011](posix-utilities.md#u-011): [return](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#return), function/dot transfer, default/explicit status, parameters and traps | C `function nested parameters and return`, `return implicit status`, `return through loop case`, `return restores enclosing redirects`; E `nested dot return`, `source return inherited by subshell`; T `return in function action`. Assert status 17/12/9, no following body, parameter and descriptor restoration. | C `return status modulo` (negative operand) is a project extension, not a baseline numeric oracle. Full trap/default-status interaction retains CSH-054; out-of-context and excess operands use documented error policy. |
| <a id="u-012"></a>[U-012](posix-utilities.md#u-012): [set](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#set), quoted sorted variable listing, positional replacement/-- | S `set positional empty and clear`, `set listing round trip`; P `set C locale order and quoting` asserts Avar/Mvar/Zvar collation and escaped apostrophe. C function cases verify positional restoration. | Non-C order uses `strcoll`, with broader locale predicates in portability; explicit non-C set-listing evidence remains CSH-048. Option families remain O-001–O-018/CSH-051, not inferred from these tests. |
| <a id="u-013"></a>[U-013](posix-utilities.md#u-013): [shift](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#shift), default/zero/count and errors | S `shift default zero count` asserts all parameters then zero; `shift error 9` / `bad` assert diagnostic, status 1 and absent following file. E `command special error continues`; API builtin rows cover negative/overflow/excess without changing count. | Select fatal noninteractive error; command suppression and interactive cases continue with warning/nonzero. Does not turn the permitted-exit choice into a requirement on other shells. |
| <a id="u-014"></a>[U-014](posix-utilities.md#u-014): [times](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#times), four accumulated times and POSIX-locale format | P `times format and accumulated children` checks two samples, each exactly two lines/four minute-second pairs with six fractional digits, seconds below 60, nondecreasing values and at least 0.1 seconds newly accumulated child CPU. E `times operand error suppressed` asserts error status 2 under command. | Fix replaces three fractional digits with required `%f` formatting. Kernel times/sysconf failure injection and output-write diagnostics remain CSH-048. No fixed CPU-time oracle. |
| <a id="u-016"></a>[U-016](posix-utilities.md#u-016): [unset](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/V3_chap02.html#unset), -v/-f, absent names, readonly, environment | S `unset namespaces and absent names`, `unset readonly stops`, `import export empty and unset`; C `function unsets active body`. Assert variable/function separation, absent-name success, readonly failure and child environment removal. | Default chooses variable namespace. Combined/repeated conflicting flags and full invalid-name/operand matrix remain CSH-048. |

## Directory, lookup and stateful utilities

| Row / reviewed utility clauses | Exact witnesses and assertions | Policy / remaining limitation |
| --- | --- | --- |
| <a id="u-019"></a>[U-019](posix-utilities.md#u-019): [cd](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/cd.html), HOME/CDPATH/OLDPWD, logical/physical, -e, canonicalization, state/output/errors | S `cd HOME and OLDPWD`, `cd logical physical and option order`, `cd CDPATH output and empty component`, `cd failure preserves directories`, `cd HOME missing policy`; API `builtins.py` scripts `cd -Pe real`, `cd missing/..`, `cd file/..`, symlink/.. and readonly PWD. Assert logical-vs-physical result, last -L/-P wins, printing only for selected CDPATH/`-`, and state preserved on ordinary failure. | HOME unset/empty selects diagnostic/status 1. Application-modified PWD/OLDPWD cases are project robustness policies (source leaves behavior unspecified). Public-runtime -e failure, cwd recovery under permissions and long canonical paths remain CSH-048. |
| <a id="u-020"></a>[U-020](posix-utilities.md#u-020): [command](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/command.html), suppression, -p/-v/-V, declarations/aliases/status | E `command bypass function`, `command suppresses special assignment`, `command special error continues`, `command declaration context`, `command default PATH preserves environment`, `lookup alias function reserved`, `lookup missing`, `lookup options`, `alias quoted name not substituted`; S `command lookup status and suppression`. Assert function bypass, restored prefix, default search preserving child PATH, unsplit declaration fields, lookup classifications and nonzero absent-name status. | D-006 expanded-name declaration recognition is implementation policy; lexical `command -p export` is required. Exact -V prose and alias spelling are regression choices. Mixed lookup operand/status and all option-order cases remain CSH-048. |
| <a id="u-023"></a>[U-023](posix-utilities.md#u-023): [getopts](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/getopts.html), OPTIND/OPTARG, argument sources, errors/end/reset | E `getopts grouped and required argument`, `getopts separate argument`, `getopts explicit reset`, `getopts silent errors`, `getopts diagnostic errors`, `getopts end`, `getopts missing operands`, `getopts invalid state`, `getopts subshell cursor isolated`; S `getopts readonly processing error`, `startup OPTIND reset`. Exact tuples distinguish normal option errors (0 with ?/:), end (1), processing errors (2), unset OPTARG and reset cursor. | Changing argument lists without OPTIND=1 is unspecified, not a normative oracle. Name validation and partial assignment failures across readonly OPTARG/OPTIND remain CSH-048. |
| <a id="u-024"></a>[U-024](posix-utilities.md#u-024): [hash](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/hash.html), remember/report/reset, function/builtin exclusion, PATH change/errors | P `hash listing excludes builtin and function` checks empty listing after f/read, pathname presence after cat, empty after -r; E `hash PATH invalidates`, `hash dotted utility and stale retry`, `hash missing`, `hash invalid option`. Assert no stale PATH entry, fallback after removal and error statuses. | Listing layout/order unspecified. E's `/bin/echo` spelling remains historical project regression only. Tracking efficiency and exotic executable replacement races are not established. |
| <a id="u-027"></a>[U-027](posix-utilities.md#u-027): [read](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/read.html), -r/-d, logical line, IFS, EOF/errors/current environment/PS2 | E `read fields remainder`, `read nonwhite IFS`, `read escape protects delimiter`, `read continuation`, `read raw`, `read delimiter and next read`, `read empty delimiter`, `read EOF partial`, `read empty EOF clears names`, `read empty IFS`, `read invalid name`, `read readonly`, `read input error`, `read missing delimiter operand`; S `read fewer fields and raw continuation`; R `terminal read continuation prompt`. Assert exact assigned fields, delimiter consumption, EOF=1 versus assignment/input errors=2 and terminal continuation. | Assigning IFS as one of read's variables has unspecified splitting effects and is not given a normative oracle. Multibyte IFS cases are in portability. Full PS2 expansion, interrupted reads and allocation/partial-assignment errors remain CSH-048/054. |
| <a id="u-029"></a>[U-029](posix-utilities.md#u-029): [ulimit](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/ulimit.html), -H/-S/-a, c/d/f/n/s/v, defaults/units/inheritance | P `ulimit query c` / `d` / `f` / `n` / `s` / `v` assert two valid numeric/unlimited lines; `ulimit default and inherited units` checks default f, both limits=128, helper byte limits=65536; `ulimit independent soft hard and subshell` checks 64 child/128 parent/256 hard and helper values; `ulimit all descriptions units and values` checks each base option, description/unit and limit value. E `ulimit invalid option`, `ulimit invalid value`, `ulimit all rejects mutation` assert diagnostics/status 2. | Repeated resource options unspecified; grouped flags are an extension to required separate options. -t is XSI (implemented extension). Mutation tests do not lower limits in the runner. Setting unlimited, overflow boundaries, hard-limit permission failures and byte-unit mutation of every resource remain CSH-048. |
| <a id="u-030"></a>[U-030](posix-utilities.md#u-030): [umask](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/umask.html), numeric/symbolic, no operand/-S, files/isolation | S `umask reusable default`, `umask reusable symbolic`, four context-isolation cases; E `umask symbolic copying`, `umask error preserves state`, `umask operand count`, `umask affects creation`. Assert restoration without assuming numeric default spelling, exact -S permissions and created mode 0640. | cshell chooses four octal digits for default output; standard only requires reusable output. Full symbolic grammar/error matrix and write failures remain CSH-048. |
| <a id="u-033"></a>[U-033](posix-utilities.md#u-033): [pwd](https://pubs.opengroup.org/onlinepubs/9799919799/utilities/pwd.html), absolute/logical/physical, PWD validation, errors and non-intrinsic lookup | S `pwd logical physical option order`, `pwd output failure continues`, startup PWD cases; E `pwd follows PATH`; API `pwd -L` with `/wrong` and redirected output. Assert last -L/-P wins, symlink/physical distinctions, output failure status 1 and failure under missing PATH. | Runtime invalid-PWD initialization is required; later application changes to PWD have unspecified effects. Permission/deleted-cwd errors and all operand permutations remain CSH-048. |

## Run records

See [CSH-048 validation](tickets/CSH-048-state-builtin-evidence.md#validation-record)
and [identities and logs](evidence/csh-048/README.md). A successful shared-runner
case checks streams, status, specified files and bounded cleanup; a printed case
name alone is not the oracle. Tests of nondeterministic output use the explicit
P predicates above. Environment/capability skips remain scoped evidence gaps.
The residual items in the tables retain CSH-048 ownership unless a narrower
owner is named; they are not profile exclusions. The CSH-012 compliance gate
stays closed.
